name: Build Kernel

on:
  workflow_dispatch:
    inputs:
      TODO:
        description: "Generate"
        default: "kernel"
        type: choice
        options:
          - "kernel"
          - "defconfig"
      kernelrepo:
        description: "Kernel Repository"
        default: "main"
        type: choice
        options:
          - "main"
          - "rama"
      GKIVER:
        description: "GKI Kernel Version"
        default: "6.12"
        type: choice
        options:
          - "5.10"
          - "6.12"
      TCPCONG:
        description: "TCP Congestion Control"
        default: "bbr"
        type: choice
        options:
          - "bbr"
          - "bbrplus"
          - "westwood"
      CLANGURL:
        description: "Select Clang Version"
        default: "22"
        type: choice
        options:
          - "22"
          - "neutron"
      LTOBUILD:
        description: "Setup LTO"
        default: "thin"
        type: choice
        options:
          - "full"
          - "thin"
      VARIANT:
        description: "Kernel Variants"
        default: "SUSFS"
        type: choice
        options:
          - "VNL"
          - "KSU"
          - "SUSFS"
          - "ALL"
      CONFIGHZ:
        description: "Custom HZ"
        default: "250"
        type: choice
        options:
          - "250"
          - "300"
      build_type:
        description: "Build type"
        required: true
        default: "test"
        type: choice
        options:
          - test
          - release
      KERNELNAME:
        description: "Kernel Name"
        default: "Cinnamon"
        type: string

env:
  MAN_DISABLE: true
  DEBIAN_FRONTEND: noninteractive
  BASE_NAME: Sagiri
  BUILD_TYPE: ${{ github.event.inputs.build_type }}
  TG_BOT_TOKEN: ${{ secrets.TG_BOT_TOKEN }}
  TG_CHAT_ID: ${{ secrets.TG_CHAT_ID }}
  GH_TOKEN: ${{ secrets.YOUR_GITHUB_TOKEN }}

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        variant: ${{ inputs.VARIANT == 'ALL' && fromJson('["KSU","VNL","SUSFS"]') || fromJson(format('["{0}"]', inputs.VARIANT || 'SUSFS')) }}

    steps:
      - name: Checkout Source
        uses: actions/checkout@v4

      - name: Free Disk Space
        run: |
          sudo rm -rf /usr/share/dotnet
          sudo rm -rf /opt/ghc
          sudo apt clean

      - name: Setup Swap 20GB
        uses: pierotofy/set-swap-space@master
        with:
          swap-size-gb: 20

      - name: Install Build Dependencies
        run: |
          sudo apt-get update
          sudo apt install -y git curl bc bison unzip zip repo rsync
          pip install lxml

      - name: Run Build Script
        env:
          VARIANT: ${{ matrix.variant }}
          KERNEL_NAME: ${{ inputs.KERNELNAME }}
          REPONYA: ${{ inputs.kernelrepo }}
          GKIVER: ${{ inputs.GKIVER }}
          LTOBUILD: ${{ inputs.LTOBUILD }}
          CLANGURL: ${{ inputs.CLANGURL }}
          TCPCONG: ${{ inputs.TCPCONG }}
          CONFIGHZ: ${{ inputs.CONFIGHZ }}
          KERNELBRANCH: ${{ inputs.KERNELBRANCH }}
          TODO: ${{ inputs.TODO }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          chmod +x *.sh
          if [ -d "py" ]; then chmod +x py/*.py; fi
          ./build.sh

      - name: Upload Intermediary Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: kernel-${{ matrix.variant }}
          path: artifacts/*.zip
 
  release:
    needs: [build]
    runs-on: ubuntu-latest
    if: ${{ github.event.inputs.build_type == 'release' }}
    permissions:
      contents: write
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Download All Matrix Artifacts
        uses: actions/download-artifact@v4
        with:
          path: all-artifacts
          pattern: kernel-*
          merge-multiple: true

      - name: Create Unified GitHub Release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          BUILD_TIME=$(date +"%Y%m%d-%H%M")
          KERNEL_NAME="${{ inputs.KERNELNAME || 'Kinosaki' }}"

          RELEASE_TAG="${KERNEL_NAME}-${BUILD_TIME}"

          echo "Creating Release Tag: $RELEASE_TAG"

          gh release create "$RELEASE_TAG" \
            --repo "rinnsakaguchi/builder" \
            --title "$KERNEL_NAME | $BUILD_TIME" \
            --notes "Automated release build at $BUILD_TIME." || true

          sleep 3

          gh release upload "$RELEASE_TAG" all-artifacts/*.zip \
            --repo "rinnsakaguchi/builder" \
            --clobber

      - name: Send Release Notification to Telegram
        run: |
          BUILD_TIME=$(date +"%Y%m%d-%H%M")
          RELEASE_TAG="${{ inputs.KERNELNAME || 'Kinosaki' }}-${BUILD_TIME}"

          RELEASE_URL="https://github.com/rinnsakaguchi/builder/releases/tag/$RELEASE_TAG"

          MSG="🚀 *New Release Published!*%0A%0A📱 *Kernel*: \`${{ inputs.KERNELNAME }}\`%0A🔖 *Tag*: \`$RELEASE_TAG\`%0A%0A🔗 [Download Here]($RELEASE_URL)"

          curl -s -X POST "https://api.telegram.org/bot${{ secrets.TG_BOT_TOKEN }}/sendMessage" \
            -d "chat_id=${{ secrets.TG_CHAT_ID }}" \
            -d "message_thread_id=7" \
            -d "parse_mode=markdown" \
            -d "text=$MSG"
